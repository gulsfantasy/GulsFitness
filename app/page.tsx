import Homepage from "@/app/Components/Homepage/Homepage";
import React from "react";

const page = async () => {
  return (
    <div  className="bg-gradient-to-r from-muted to-black min-h-screen">
      <Homepage />
    </div>
  );
};

export default page;
